# In your terminal, navigate to your project folder
cd nifty-trading-platform

# Initialize git
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit - Nifty trading platform"

# Add your GitHub repository (replace YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/nifty-trading-platform.git

# Push to GitHub
git branch -M main
git push -u origin main
